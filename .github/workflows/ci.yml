name: CI

on:
  push:
    branches: [main, agent/release-3.3.2-complete-consistency]
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare-release-3-3-2-docs:
    if: github.event_name == 'push' && github.ref == 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/release-3.3.2-complete-consistency
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - name: Apply complete current-document synchronization
        run: |
          node scripts/prepare-release-3.3.2-current-docs.cjs
          node scripts/prepare-release-3.3.2-current-docs-extra.cjs
      - run: npm ci
      - run: node --check scripts/check-release-consistency.cjs
      - run: npm run release:consistency
      - run: node --test test/release-consistency.test.cjs
      - run: node --check website/app.js && node --check website/site-fixes.js && node website/validate.mjs
      - name: Commit synchronized branch and remove one-shot materializer
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main
          git checkout origin/main -- .github/workflows/ci.yml
          git rm scripts/prepare-release-3.3.2-current-docs.cjs scripts/prepare-release-3.3.2-current-docs-extra.cjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(release): complete Nexora 3.3.2 current consistency"
          git fetch origin agent/release-3.3.2-complete-consistency
          git rebase origin/agent/release-3.3.2-complete-consistency
          git push origin HEAD:agent/release-3.3.2-complete-consistency

  verify:
    if: github.event_name != 'push' || github.ref != 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: windows-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run test:unit
      - run: npm run test:performance
      - run: npm run audit:security

  linux-tests:
    if: github.event_name != 'push' || github.ref != 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - run: npm test

  release-gate:
    if: github.event_name != 'push' || github.ref != 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - run: npm run release:check

  schema8-soak:
    if: github.event_name != 'push' || github.ref != 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - run: npm run test:soak
        env:
          NEXORA_SOAK_MINUTES: "1"
          NEXORA_SOAK_INTERVAL_MS: "500"

  android-source:
    if: github.event_name != 'push' || github.ref != 'refs/heads/agent/release-3.3.2-complete-consistency'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"
      - run: gradle -p android :app:assembleDebug --no-daemon
