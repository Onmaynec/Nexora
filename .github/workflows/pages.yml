name: Project websites

on:
  push:
    branches: [main]
    paths:
      - "website/**"
      - "advanced-website/**"
      - "server/**"
      - "cloud/**"
      - "package.json"
      - "package-lock.json"
      - "docs/releases/**"
      - ".github/workflows/pages.yml"
  pull_request:
    paths:
      - "website/**"
      - "advanced-website/**"
      - "server/**"
      - "cloud/**"
      - "package.json"
      - "package-lock.json"
      - "docs/releases/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Validate introductory website
        run: |
          node --check website/release-fallback.js
          node --check website/network-resilience.js
          node --check website/app.js
          node --check website/site-fixes.js
          node --check website/site-polish.js
          node website/generate-release-fallback.mjs --check
          node --test website/release-resilience.test.cjs
          node website/validate.mjs
          node website/validate-restored.mjs
      - name: Compose introductory website artifact
        shell: bash
        run: |
          set -euo pipefail
          printf '\n\n' >> website/styles.css
          cat website/site-fixes.css >> website/styles.css
          printf '\n\n' >> website/styles.css
          cat website/site-polish.css >> website/styles.css
          {
            cat website/release-fallback.js
            printf '\n\n'
            cat website/network-resilience.js
            printf '\n\n'
            cat website/app.js
            printf '\n\n'
            cat website/site-fixes.js
            printf '\n\n'
            cat website/site-polish.js
          } > website/app.composed.js
          mv website/app.composed.js website/app.js
          node --check website/app.js
          grep -Fq 'NexoraReleaseFallback' website/app.js
          grep -Fq 'RELEASE_REQUEST_TIMEOUT_MS' website/app.js
          grep -Fq 'class AetherField' website/app.js
          grep -Fq 'main-site-ux-polish-release-resilience-20260724-2' website/build.json
          test -s website/UX_AUDIT_2026-07-24.md
          node advanced-website/scripts/inject-entry-link.mjs website/index.html
          test "$(grep -Fc 'data-advanced-docs' website/index.html)" -eq 1
      - name: Generate advanced reference
        run: node advanced-website/scripts/generate-reference.mjs
      - name: Validate advanced documentation
        run: |
          node advanced-website/scripts/validate-content.mjs
          node --test advanced-website/test/*.test.mjs
      - name: Build advanced React portal
        run: npx vite build --config advanced-website/vite.config.mjs
      - name: Verify composed artifact
        shell: bash
        run: |
          set -euo pipefail
          test -s website/index.html
          test -s website/advanced/index.html
          grep -Fq 'Nexora Advanced Documentation' website/advanced/index.html
          grep -Fq '/Nexora/advanced/assets/' website/advanced/index.html
          test -s website/advanced/robots.txt
      - uses: actions/upload-artifact@v4
        with:
          name: nexora-pages-site
          path: website
          if-no-files-found: error
          retention-days: 2

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 12
    concurrency:
      group: github-pages-production
      cancel-in-progress: true
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: nexora-pages-site
          path: _site
      - uses: actions/configure-pages@v6
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
      - name: Smoke-check published sites
        shell: bash
        run: |
          set -euo pipefail
          base="${{ steps.deployment.outputs.page_url }}"
          for attempt in $(seq 1 30); do
            root="$(curl --fail --silent --show-error --location --header 'Cache-Control: no-cache' "${base}build.json?attempt=${attempt}" || true)"
            advanced="$(curl --fail --silent --show-error --location --header 'Cache-Control: no-cache' "${base}advanced/?attempt=${attempt}" || true)"
            if grep -Fq 'main-site-ux-polish-release-resilience-20260724-2' <<<"$root" && grep -Fq 'Nexora Advanced Documentation' <<<"$advanced"; then
              echo "Both Nexora websites verified at ${base} and ${base}advanced/"
              exit 0
            fi
            sleep 10
          done
          echo "Published Pages artifact did not expose both websites within the verification window." >&2
          exit 1
