name: Report Nexora 3.1.2 release-check tail

on:
  push:
    branches:
      - agent/nexora-3.1.2-bugfix

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  report-tail:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/nexora-3.1.2-bugfix
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - name: Run complete release gate and publish tail
        shell: bash
        run: |
          set +e
          {
            node scripts/apply-3.1.2.cjs
            node scripts/sync-release-metadata.cjs
            npm ci
            npm run release:check
          } > /tmp/release-check.log 2>&1
          code=$?
          {
            echo '### 3.1.2 release-check tail'
            echo
            echo '```text'
            tail -n 100 /tmp/release-check.log
            echo '```'
            echo
            echo "exit_code=$code"
          } > /tmp/comment.md
          gh pr comment 9 --repo Onmaynec/Nexora --body-file /tmp/comment.md
          exit "$code"
