# Nexora project website

Static presentation site for Nexora.

## Preview

Run `python -m http.server 8080` from the `website` directory and open `http://localhost:8080`.

## GitHub Pages

The workflow in `.github/workflows/pages.yml` validates and publishes this directory after changes reach `main`. Configure the repository Pages source as GitHub Actions.
